Excluding .idea/ because it is IntelliJ project structure and setting files which other might not use it.
Excluding target/ because it contain compiled classes and jar file which other probably will not using it.
Excluding *.iml, excluding all file with extension .iml because it is a module file created by IntelliJ.
It stores information about a development module which other probably will not using it.  
